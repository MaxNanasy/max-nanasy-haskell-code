module Language.Functional.Test where
    {
      module Nat where
          {
            codata Nat a b = Zero | Succ Nat;
          };
      module Main where
          {
            import Nat;

            main = Zero 0 inc;
          };
    };
